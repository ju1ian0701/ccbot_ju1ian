# Чек-лист п. 3 — smoke ISS-020

**Product:** PR #33 `3e06992` (`fix: broaden allowed_updates + edit-guards`)  
**Статус:** blocked — требуется выделенный сервер с живой tmux-сессией (`ccbot:__main__`).  
**Отчёт попытки 2026-08-14:** `D:\GROK\reports\2026-08-14-iss-020-smoke.md`

Подготовка: `git pull` до `3e06992`; `./scripts/restart.sh`; в логах старт с нового кода; tail лога открыт на время теста.

## Тест 1 — delivery forum service messages

- Действие: переименовать топик в тестовой forum-группе (с привязанным окном).
- Ожидание: `topic_edited_handler` срабатывает — событие в логе (`Topic renamed:`), поведение привязки штатное.
- PASS: событие дошло и обработано.
- FAIL: тишина в логе → эмпирическая посылка PR #86 не подтвердилась на окружении → стоп, доклад, откат не делать молча.

## Тест 2 — edit-guard

- Действие: отправить текстовое сообщение в привязанный топик → дождаться инъекции в tmux; затем отредактировать это сообщение.
- Ожидание: на отправку — одна инъекция; на редактирование — ноль новых событий (в логе нет второго срабатывания `text_handler` / второго `send_to_window`; в pane — одна копия текста).
- PASS: дубля нет.
- FAIL: повторная инъекция → guard не работает (сначала проверить, что рестарт был с новым кодом) → стоп, доклад.

## Тест 3 — регрессия обычного потока

- Действие: обычное сообщение + одна callback-кнопка (любой picker) в топике.
- Ожидание: штатная обработка обоих — расширение подписки не сломало базовый путь.
- PASS: оба пути живые.
- FAIL: стоп, доклад.

## Протокол

По каждому тесту — excerpt лога (2–5 строк). Все три PASS → smoke закрыт. Любой FAIL → ничего не править, доклад с логом.
